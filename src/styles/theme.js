import { theme as antdTheme } from 'antd';

export const theme = {
  algorithm: antdTheme.defaultAlgorithm, // Ant Design 알고리즘 사용
  token: {
    colorPrimary: '#3498db', // 기존 primary 컬러 반영
    colorTextDisabled: '#bdc3c7',
    fontSize: 16,
    borderRadius: 6,
  },
};
